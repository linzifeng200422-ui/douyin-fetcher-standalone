# 抖音下载器 核心依赖
aiohttp>=3.9.0
aiofiles>=23.2.1
aiosqlite>=0.19.0
rich>=13.7.0
pyyaml>=6.0.1
python-dateutil>=2.8.2
gmssl>=3.2.2

# HTTP API Server（可选）
fastapi>=0.100.0
uvicorn>=0.20.0

# 以下为可选依赖，按需安装：
# ASR 台词转写：pip install openai-whisper
# 浏览器兜底：pip install playwright && playwright install chromium
# 系统级：brew install ffmpeg
